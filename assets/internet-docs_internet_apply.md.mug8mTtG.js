import{_ as t,c as a,o as s,U as e}from"./chunks/framework.PFQuerqu.js";const o="/assets/dns-server.G9Ka7_23.png",r="/assets/dns-search.MwWRno_4.png",i="/assets/http-status-code.o18gdst9.png",l="/assets/keep-alive.YEfjf6ko.png",p="/assets/keep-channel.of6sK6vJ.png",n="/assets/队头阻塞.TnidqKop.png",T="/assets/重定向-1.5lehyWJh.png",d="/assets/重定向-2.XZrhgny-.png",c="/assets/https-good.bY2dj7KR.png",h="/assets/混合加密.uPZG2XUf.png",g="/assets/https-1.Ft5bD9sr.png",P="/assets/https-2.8hjaRKt3.png",m="/assets/http-3.fzrtxiNU.png",u="/assets/http-4.0E07Xydd.png",H="/assets/rsa-client-hello.NAziMCVr.png",b="/assets/ras-server-hello.zF_KX7v6.png",S="/assets/https-other.gwrQybUa.png",k="/assets/head-press.qdCbJ_S0.png",_="/assets/frame-2.HWtolR9b.png",q="/assets/frame-3.HMEiJeip.png",C="/assets/frame-4.7fh-89bo.png",f="/assets/frame-5.CJQpFRrx.jpeg",I="/assets/frame-6.EtTMT3Vm.png",v="/assets/frame-7.IFMh4rrz.png",x="/assets/tcp98E5B498E59E.cfzCb3Xm.gif",D="/assets/frame-8.br-shwDu.png",y="/assets/frame-9.asjQkmsU.png",E="/assets/frame-10.8u9c7exY.png",z=JSON.parse('{"title":"应用层","description":"","frontmatter":{},"headers":[],"relativePath":"internet-docs/internet/apply.md","filePath":"internet-docs/internet/apply.md","lastUpdated":1711901311000}'),L={name:"internet-docs/internet/apply.md"},U=e('<h1 id="应用层" tabindex="-1">应用层 <a class="header-anchor" href="#应用层" aria-label="Permalink to &quot;应用层&quot;">​</a></h1><h2 id="域名系统dns" tabindex="-1">域名系统DNS <a class="header-anchor" href="#域名系统dns" aria-label="Permalink to &quot;域名系统DNS&quot;">​</a></h2><p>DNS（Domain Name System）是因特网使用的命名系统，用来把域名转换为IP地址。</p><p>从理论上讲，整个互联网可以只是用一个域名服务器，然而一旦这个域名服务器崩了，那么整个网络就会瘫痪了。</p><p>所以互联网的域名系统被设计为了一个联机分布式数据库系统。DNS使大多数名字都在本地进行解析（resolve），仅仅使用少量解析需要在因特网上通信。</p><p>域名系统的过程的简单概括：当某一个应用进程需要把主机名解析为IP地址时，这个应用进程就调用解析程序，并称为DNS的一个客户，把待解析的域名放在DNS请求报文中，以UDP用户数据报方式发给本地域名服务器。本地域名服务器在查找域名后，把对应的IP地址放在回答报文中返回。应用经常获得目的主机的IP地址后即可进行通信。</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>如果本地域名服务器不能回答这个请求，则此域名服务器就暂时称为DNS中的另一个客户，并向其他域名服务器发出查询请求。这种过程直到找到能够回答请求的域名服务器为止</p></div><h2 id="域名服务器" tabindex="-1">域名服务器 <a class="header-anchor" href="#域名服务器" aria-label="Permalink to &quot;域名服务器&quot;">​</a></h2><p>互联网上的DNS域名服务器也是按照层次来进行安排的。每一个域名服务器都只对域名体系种的一部分进行管辖： <img src="'+o+'" alt="image"></p><p>根据这些域名服务器所起到的作用，只需要把域名服务器划分为以下四种不同的类型：<strong>根域名服务器</strong> <strong>顶级域名服务器</strong> <strong>权限域名服务器</strong> <strong>本地域名服务器</strong></p><h3 id="根域名服务器" tabindex="-1">根域名服务器 <a class="header-anchor" href="#根域名服务器" aria-label="Permalink to &quot;根域名服务器&quot;">​</a></h3><p>根域名服务器是最高层次的域名服务器，所有根域名服务器都知道所有顶级域名服务器的域名和IP地址。</p><p>不管是哪个本地域名服务器，只要自己无法解析，首先要求于根域名服务器。假定所有根域名服务器都瘫痪了，那么整个DNS系统就无法工作。</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>互联网上一共有13个不同IP地址的根域名服务器，它们的名字是用一个英文字母命名，从a一直到m。这些根域名服务器相应的域名分别为<code>a.rootservers.net</code>，<code>...</code>，<code>m.rootservers.net</code></p></div><h3 id="顶级域名服务器" tabindex="-1">顶级域名服务器 <a class="header-anchor" href="#顶级域名服务器" aria-label="Permalink to &quot;顶级域名服务器&quot;">​</a></h3><p>这些域名服务器负责管理在该顶级域名服务器注册的所有二级域名。当收到DNS查询时，就会给出相应的回答</p><h3 id="权限域名服务器" tabindex="-1">权限域名服务器 <a class="header-anchor" href="#权限域名服务器" aria-label="Permalink to &quot;权限域名服务器&quot;">​</a></h3><p>这就是前面已经讲过的负责一个区的域名服务器。当一个权限域名服务器还不能给出最后的查询回答时，就会告诉发出查询请求DNS客户，下一步应当找哪一个权限域名服务器</p><h3 id="本地域名服务器" tabindex="-1">本地域名服务器 <a class="header-anchor" href="#本地域名服务器" aria-label="Permalink to &quot;本地域名服务器&quot;">​</a></h3><p>本地域名服务器并不属于上图的域名结构，但是它对于域名系统来说是非常重要的。当一个主机发出DNS查询请求时，这个查询请求报文就发松给本地域名服务器。</p><p>本地域名服务器离用户较近，一般不超过几个路由器的距离。</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>为了提高域名服务器的可靠性，DNS域名服务器都把数据复制到几个域名服务器来保存，其中的一个是主域名服务器，其他的是辅助域名服务器。这样保证出现故障时有备用服务器。</p></div><h2 id="域名解析过程" tabindex="-1">域名解析过程 <a class="header-anchor" href="#域名解析过程" aria-label="Permalink to &quot;域名解析过程&quot;">​</a></h2><h3 id="递归查询" tabindex="-1">递归查询 <a class="header-anchor" href="#递归查询" aria-label="Permalink to &quot;递归查询&quot;">​</a></h3><p>主机向本地域名服务器的查询采用递归查询。</p><p>递归查询就是：如果主机所询问的本地域名服务器不知道被查询域名的IP地址，那么本地域名服务器就以DNS客户的身份，向其他根域名服务器继续发出查询请求报文（<strong>即替该主机继续查询</strong>），而不是让该主机自己进行下一步的查询。因此，递归查询返回的查询结果或者所要查询的IP地址，或者报错，表示无法查询到所需的IP地址。</p><h3 id="迭代查询" tabindex="-1">迭代查询 <a class="header-anchor" href="#迭代查询" aria-label="Permalink to &quot;迭代查询&quot;">​</a></h3><p>本地域名服务器向根域名服务器的查询通常是采用迭代查询。</p><p>迭代查询的特点是：当根域名服务器收到本地域名服务器发出的迭代查询请求报文时，要么给出所查询的IP地址，要么告诉本地域名服务器：<strong>下一步应当向哪一个域名服务器进行查询。</strong></p><p>然后让本地域名服务器进行后续的查询：</p><ul><li>根域名服务器通常把自己知道的顶级域名服务器的IP地址告诉本地域名服务器，让本地域名服务器再向顶级域名服务器查询</li><li>顶级域名服务器在收到本地域名服务器的查询请求后，要么给出所要查询的IP地址，要么告诉本地域名服务器下一步应当向哪一个域名服务器进行查询</li></ul><p>最后知道了所要解析的域名的IP地址，然后把这个结果返回给所查询的主句：</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>当然，本地域名服务器也可以采用递归查询，这取决于最初的查询请求报文的设置是要求使用哪一种查询方式。</p></div><p><img src="'+r+'" alt="image"></p><p>为了提高DNS查询效率，并减轻根域名服务器的负荷的减少互联网的DNS查询报文数量，在域名服务器中广泛地使用了高速缓存，**有时也称为高速缓存域名服务器。**高速缓存用来存放最近查询过的域名以及从何处获得域名映射信息的记录。</p><h2 id="http协议" tabindex="-1">HTTP协议 <a class="header-anchor" href="#http协议" aria-label="Permalink to &quot;HTTP协议&quot;">​</a></h2><p>HTTP是超文本传输协议，是用于在两点之间进行超文本传输的协议</p><h3 id="http状态码" tabindex="-1">HTTP状态码 <a class="header-anchor" href="#http状态码" aria-label="Permalink to &quot;HTTP状态码&quot;">​</a></h3><p>HTTP状态码来表示本次HTTP传输的结果状态 <img src="'+i+'" alt="image"> 以下是一些常见的状态码列表和解释</p><table><thead><tr><th>编码</th><th>含义</th></tr></thead><tbody><tr><td>200 OK</td><td>表示一起正常，只要是非<code>HEAD</code>请求，服务器返回的响应头都会有body数据</td></tr><tr><td>204 No Content</td><td>表示一起正常，只要是非HEAD请求，服务器返回的响应头都会有body数据</td></tr><tr><td>206 Partial Content</td><td>是其中的一部分，也是服务器处理成功的状态</td></tr><tr><td>301 Move Parmanently</td><td>表示永久重定向，说明请求的资源已经不在了，需要该用新的URL再次访问</td></tr><tr><td>302 Found</td><td>表示临时重定向，说明请求的资源还在，但暂时需要用另一个URL来访问</td></tr><tr><td>304 Not Modified</td><td>304 Not Modified 表示资源为修改，重定向已存在的缓冲文件，也称为缓存重定向，用于协商缓存</td></tr><tr><td>400 Bad Request</td><td>表示客户端发送的请求报文存在错误，一般来说是参数发错了～</td></tr><tr><td>403</td><td>表示服务器禁止客户端来访问资源，并不是客户端的请求出错了</td></tr><tr><td>404</td><td>表示请求的资源在服务器上找不到，或者不存在了</td></tr><tr><td>500</td><td>服务器发生内部错误</td></tr><tr><td>501</td><td>表示客户端请求的功能还不支持，类似“即将开业，敬请期待”的意思</td></tr><tr><td>502</td><td>通常是服务器作为网关或代理时返回的错误码，表示服务器自身工作正常，访问后端服务器发生了错误</td></tr><tr><td>503</td><td>表示服务器当前很忙，暂时无法响应客户端，类似“网络服务很忙，稍后重试”</td></tr></tbody></table><h3 id="get请求与post请求的区别" tabindex="-1">GET请求与POST请求的区别 <a class="header-anchor" href="#get请求与post请求的区别" aria-label="Permalink to &quot;GET请求与POST请求的区别&quot;">​</a></h3><p>根据RFC规范，<strong>GET的语义时从服务器获取指定的资源</strong>，这个资源可以是静态的文本，页面，图片视频等。GET请求的参数位置一般写在URL中，URL规定只能支持ASCII，所以GET请求的参数只允许ASCII字符，并且浏览器会URL的长度有限制（HTTP协议本身没有对URL做限制）</p><p>而<strong>POST的语义上根据请求负荷（报文body）对指定的资源做出处理</strong>，具体的处理方式视资源类型而不同。POST请求携带数据的位置一般是写在报文body中，body中的数据可以是任意格式的数据，只要客户端与服务端协商好，而且浏览器不会对body大小做限制。</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>对于post来说，浏览器认为这不是一个简单请求，在浏览器会在发出真正请求之前，先发出<strong>预检请求</strong>，检查当前请求是否符合服务器的cors配置，如果符合，则再发出真正的请求，否则直接返回跨域报错。<strong>如果预检请求通过了，在一定时间内重复请求是不用再次发起预检请求了</strong>。</p></div><h2 id="http版本" tabindex="-1">HTTP版本 <a class="header-anchor" href="#http版本" aria-label="Permalink to &quot;HTTP版本&quot;">​</a></h2><p>到目前为止，HTTP的常见版本有 HTTP/1.1，HTTPS，HTTP/2.0，HTTP/3.0，不同版本的HTTP特性是不一样的。</p><h3 id="http-1-1" tabindex="-1">HTTP/1.1 <a class="header-anchor" href="#http-1-1" aria-label="Permalink to &quot;HTTP/1.1&quot;">​</a></h3><p>其实 HTTP/1.1 是由 HTTP/2.0 演变过来的，主要有这些性能上的改进：</p><ul><li>使用长链接的方式改善了 HTTP/1.0 短连接造成的性能开销</li><li>支持管道（pipeline）网络传输，只要第一个请求发出去了，不必等其回来，就可以发第二个请求出去，减少了整体的响应时间</li></ul><p>接下来从HTTP/1.1的优点，缺点，性能三个方面介绍这个协议</p><h4 id="http-1-1优点" tabindex="-1">HTTP/1.1优点 <a class="header-anchor" href="#http-1-1优点" aria-label="Permalink to &quot;HTTP/1.1优点&quot;">​</a></h4><p>HTTP/1.1 最突出的优点就是<strong>简单，灵活和易于扩展，应用广泛并跨平台</strong></p><ul><li>简单：HTTP报文格式就是<code>header + body</code>，头部信息也是<code>key-value</code>简单文本的形式</li><li>易于扩展：HTTP协议里的各类请求方法，URL/URI，状态码，头字段等每个组成要求都没有被固定死，都允许开发人员自定义和扩充。比如HTTP由于随工作在应用层，所以它的<strong>下层可以随意变化</strong>：HTTPS就是HTTP与TCP层之间增加了SSL/TLS安全传输层。</li><li>先天的跨平台优势</li></ul><h4 id="http-1-1缺点" tabindex="-1">HTTP/1.1缺点 <a class="header-anchor" href="#http-1-1缺点" aria-label="Permalink to &quot;HTTP/1.1缺点&quot;">​</a></h4><p>HTTP不好的地方在于它的无状态和明文传输</p><ul><li>无状态：无状态准确来说不是完全的缺点，它也存在优点，因为服务器不会去记忆HTTP的状态，所以不需要额外的资源来记录状态信息，所以减轻了服务器的负担；它的缺点在于由于它的无状态，当业务需求需要用户信息时，需要额外的操作来满足：Cookie + Session</li><li>明文传输：明文传输，导致Wrieshark这样可以抓包来直接肉眼查看。这导致不安全的因素，内容可能被窃听</li></ul><h4 id="http-1-1的性能" tabindex="-1">HTTP/1.1的性能 <a class="header-anchor" href="#http-1-1的性能" aria-label="Permalink to &quot;HTTP/1.1的性能&quot;">​</a></h4><p>HTTP/1.1的性能还是相当不错的，它增加了下面三点主要内容：</p><p><strong>[长连接]</strong>： 早期HTTP/1.0性能上有一个很大的问题：那就是每一次请求都要新建一次TCP连接，而且是串行请求。为了解决这个TCP连接问题，HTTP/1.1提出了<strong>长连接</strong>的通信方式，也就是持久连接。</p><p><img src="'+l+'" alt="image"></p><p>持久连接的特点在于：只要任意一端没有明确提出断开连接，则保持TCP连接状态，当然某个HTTP长连接超过一定时间没有任何数据交互，服务端就会主动断开这个连接</p><p>开启长连接会在请求和响应的报文头中显示:<code>Connection:keep-alive</code></p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>早期设计为短连接的原因在于：早期的浏览器模式一般用于浏览一些信息，当网页内容加载完毕之后，用户可能需要花费几分钟甚至更多的时间去浏览网页的内容，此时完全没有必要继续维持底层连接。当用户访问其他网页时，再创建新的链接即可。然后新的浏览器模式下，文本内容和资源巨增，如果每请求一个资源，就创建一个链接然后关闭，代价确实太大了。</p></div><p><strong>[管道网络传输]</strong>：HTTP/1.1采用了长连接的方式，使得管道（pipeline）网络传输成为了可能。</p><p>即可在同一个TCP连接里面，客户端可以发起多个请求，只要第一个请求发出去了，不必等待其回来，就可以发第二个请求出去，可以<strong>减少整体的响应时间</strong>。</p><p><img src="'+p+'" alt="image"></p><p>当然<strong>服务器也必须按照接收请求的顺序发送这些管道化请求的响应</strong>。如果服务端在处理A请求时耗时比较长，那么后续的请求处理都会被阻塞住，这就是“队头阻塞”。</p><p><strong>[队头阻塞]</strong>：请求-应答 的模式会造成 HTTP 的性能问题：因为当顺序发送的请求序列中的一个请求因为某种原因被阻塞时，在后面排队的所有请求也一同被阻塞了，会招致客户端一直请求不到数据 <img src="'+n+'" alt="image"></p><h4 id="http-1-1优化" tabindex="-1">HTTP/1.1优化 <a class="header-anchor" href="#http-1-1优化" aria-label="Permalink to &quot;HTTP/1.1优化&quot;">​</a></h4><p>根据HTTP/1.1的基本设计思路，那么可以向下面三个点来对HTTP/1.1进行优化：</p><ul><li>尽量避免发送HTTP请求</li><li>在需要发送HTTP请求时，考虑减少请求次数</li><li>减少服务器的HTTP响应的数据大小</li></ul><p><strong>[避免发送HTTP请求]</strong>:对于一些具有重复性的HTTP请求，比如每次请求得到的数据都是一样的，那么就可以对“请求-响应”的数据都缓存在本地，那么下次就直接读取本地的数据，不必再通过网络获取服务器的响应了，那么这就和浏览器的缓存策略有关了，具体可以参考 <a href="https://www.yuque.com/await-6vbwx/cy7rpt/mrkl0of4hiep2t4m" target="_blank" rel="noreferrer">浏览器缓存</a> 这篇文章</p><p><strong>[减少HTTP请求次数]</strong>：第一方面，可以减少重定向次数：服务器上的有一个资源可能由于迁移，维护等原因从 url1 移到 url2 ，而客户端不知情，他还是请求url1，此时服务器不能粗暴地返回错误，而是通过<strong>302</strong>响应吗和<strong>Location</strong>头部，通知客户端该资源已经迁移到了url2上了，于是客户端需要再发送url2请求来获取服务器的资源。</p><p>那么就要多次发起HTTP请求，每一次的HTTP请求都需要经过网络，这会导致网络性能降低。</p><p>另外服务端着一方往往不只有一台服务器，比如源服务器上一级上代理服务器，然后代理服务器才与客户端通信，这是重定向就会导致客户端与代理服务器之间需要2次消息传递：</p><p><img src="'+T+'" alt="image"></p><p>如果重定向的工作由代理服务器完成，那么就能减少HTTP请求次数：</p><p><img src="'+d+'" alt="image"></p><p>第二方面，可以合并请求，最经典的例子就是CSS的精灵图，也可以将图片的二进制数据base64编码之后嵌入html文件中并跟随发送，这样就减少了请求图片的次数。或者借助打包工具将多个小的js文件打包成体积更大的文件。</p><p>第三方面，延迟发送请求，按需获取就是这一思路，比如分页加载图片时，下一页的图片不必要第一时间加载出来。</p><p><strong>[减小HTTP请求数据]</strong>：第一方面，可以考虑无损压缩，比如gizp，客户端支持的压缩算法，会在HTTP请求中通过头部中的<code>Accept-Encoding</code>字段告诉服务器</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Accept</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Encoding</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: gzip, deflate, br</span></span></code></pre></div><p>服务器收到之后，会选择一个服务器支持的或者合适的压缩算法，然后使用此压缩算法对响应资源进行压缩，最后通过响应头部中<code>Content-Encoding</code>字段告诉客户端该资源使用的压缩算法:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Content</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Encoding</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: gzip</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>gzip的压缩效率相比Google推出的Brotli算法还是差点意思，如果可以推荐服务器选择压缩效率更高的br的压缩算法</p></div><p>第二方面，可以对一图片资源考虑有损压缩</p><h3 id="https" tabindex="-1">HTTPS <a class="header-anchor" href="#https" aria-label="Permalink to &quot;HTTPS&quot;">​</a></h3><h4 id="https-与-http-区别" tabindex="-1">HTTPS 与 HTTP 区别 <a class="header-anchor" href="#https-与-http-区别" aria-label="Permalink to &quot;HTTPS 与 HTTP 区别&quot;">​</a></h4><ul><li>HTTP是超文本传输协议，信息是明文传输，存在安全风险的问题。HTTPS则解决HTTP不安全的缺陷，在TCP和HTTP网络层之间加入了SSL/TLS安全协议，使得报文能够加密传输</li><li>HTTP连接建立相对简单，TCP三次握手之后便可进行HTTP的报文传输。而HTTPS在TCP三次握手之后，还需要建立SSL/TLS握手过程，才可以进行加密报文传输</li><li>两者的默认端口不一样，HTTP默认端口号是80，HTTPS默认端口号是443</li><li>HTTPS协议需要向CA（证书权威机构）申请数字证书，来保证服务器的身份是可靠可信的</li></ul><h4 id="https的优势" tabindex="-1">HTTPS的优势 <a class="header-anchor" href="#https的优势" aria-label="Permalink to &quot;HTTPS的优势&quot;">​</a></h4><p><img src="'+c+'" alt="image"> 由于HTTP是明文传输的，所以安全上存在一些风险。</p><p>而HTTPS在HTTP与TCP层之间加入了<code>SSL/TLS</code>协议来解决这些风险：</p><ul><li><strong>信息加密</strong>：交互的信息被加密 ---- 混合加密实现</li><li><strong>校验机制</strong>：无法篡改通信内容，只有篡改了就不能正常显示 ---- 摘要算法 + 数字签名</li><li><strong>身份证书</strong>：证明你访问的服务器是你想访问的服务器 ---- 数字证书</li></ul><p><strong>[混合加密]</strong>：HTTPS采用了对称加密和非对称加密的结合的【混合加密】方式</p><ul><li>在通信建立前采用非对称加密的方式来交换对称加密的公钥</li><li>在通信过程中全部采用对称加密的公钥的方式来加密明文数据 之所以采用[混合加密]的方式的原因在于：</li><li>对称加密只使用一个密钥，运算速度很快，密钥必须保密，无法做到安全的密钥交换</li><li>非对称加密使用两个密钥：公钥和私钥，公钥可以任意分发但是私钥必需保密，解决了密钥交换问题但是速度较慢 <img src="'+h+'" alt="image"></li></ul><p><strong>[摘要算法 + 数字签名]</strong> 为了保证传输的内容不被篡改，需要对内容计算出一个<strong>哈希值</strong>，然后同内容一起传输给对方。</p><p>对方收到之后，先是对内容也计算出一个<strong>哈希值</strong>，然后跟发送方发送的<strong>哈希值</strong>做一个比较，如果<strong>哈希值</strong>相同，说明内容没有被篡改，否则就可以判断出内容已经被篡改了。</p><p><img src="'+g+'" alt="image"></p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>通过哈希算法可以确保内容不会被篡改，<strong>但是并不能保证【内容 + 哈希值】不会被中间人替换，因为缺少对客户端收到的消息是否来源于服务端的证明</strong></p></div><p>为了避免这种情况，计算机里会用<strong>非对称加密算法</strong>来解决，共有两个密钥：</p><ul><li>一个是公钥，这个是可以公开给所有人的</li><li>一个是私钥，这个必须由本人管理，不可泄露 这个两个密钥是可以<strong>双向加解密</strong>的，比如可以用公钥加密内容，然后私钥解密；也可以用私钥加密内容，公钥解密，流程不同，意味着目的上也有不同：</li><li><strong>公钥加密，私钥解密</strong>。这个目的是为了保证内容传输的安全</li><li><strong>私钥加密，公钥解密</strong>。这个是为了保证消息不会被冒充</li></ul><p>一般情况下，都不会使用非对称加密来加密实际的传输内容，因为非对称加密的计算是比较消耗性能的。</p><p>所以非对称加密的用途主要还是在于<strong>通过【私钥加密，公钥解密】的方式，来对确认的消息的安全性</strong>，这也是数字签名算法的底层逻辑，不过私钥加密的内容不是内容本身，而是<strong>对内容的哈希值加密</strong><img src="'+P+'" alt="image"> 私钥由服务端保管，然后服务端会向客户端颁发对应的公钥。</p><p><strong>[数字证书]</strong> 通过上文可以知道：</p><ul><li>可以通过哈希算法来确认内容的安全性</li><li>通过数字签名来保证消息的来源可靠性</li></ul><p>但还缺了一步，那就是公钥被第三方拦截了并进行了伪造，那么客户端发送的消息用了盗版公钥进行加密，而服务端的私钥解密不了，但第三方却能成功解密</p><p>在计算机世界里，有一个权威机构CA（数字证书认证机构，CA就是网络世界里的公安局，公证中心），将服务器公钥放在数字证书中，只要证书可信，那么公钥也是完全可信的。</p><p><img src="'+m+'" alt="image"> 具体的做法如下图一样：</p><ul><li><ol><li>服务器把自己的公钥注册到CA</li></ol></li><li><ol start="2"><li>CA用自己的私钥将服务器的公钥数字签名并颁发数字证书</li></ol></li><li><ol start="3"><li>客户端拿到服务器的数字证书证书之后，使用CA的公钥确认服务器的数字证书是否具有真实性</li></ol></li><li><ol start="4"><li>确认可靠后，从数字证书获取服务器公钥后，使用它对报文进行加密后发送</li></ol></li><li><ol start="5"><li>服务器用私钥对报文解密即可</li></ol></li></ul><h4 id="https的连接建立" tabindex="-1">HTTPS的连接建立 <a class="header-anchor" href="#https的连接建立" aria-label="Permalink to &quot;HTTPS的连接建立&quot;">​</a></h4><p>SSL/TLS协议的基本流程：</p><ul><li>客户端向服务器端索要并验证服务器的公钥</li><li>双方协商来产生【会话密钥】</li><li>双方采用【会话密钥】进行加密通信</li></ul><p>TLS的【握手阶段】涉及到了<strong>四次</strong>通信，使用不同的密钥交换算法：</p><h2 id="" tabindex="-1"><img src="'+u+'" alt="image"> <a class="header-anchor" href="#" aria-label="Permalink to &quot;![image](./assets/http-4.png)&quot;">​</a></h2><p>TLS协议建立的详细流程如下：</p><p><strong>[ClientHello]：</strong></p><p>首先由客户端向服务器端发起加密通信请求，也就是<code>ClientHello</code>请求，在这一步客户端主要向服务器发送以下一些信息：</p><ul><li>客户端支持的TLS协议版本</li><li>客户端生产的随机数<code>Client Random</code>，后面用于生成会话密钥的条件之一</li><li>客户端支持的密码套件列表，如RSA加密算法 <img src="'+H+'" alt="image"></li></ul><p><strong>[ServerHello]</strong> 服务器收到客户端请求后，向客户端发出响应，也就是<code>ServerHello</code>。服务器返回的内容有如下：</p><ul><li>确认TLS协议版本，如果浏览器不支持，则关闭加密通信</li><li>服务器生产的随机数<code>Server Random</code>，也是后面用于生成会话密钥的条件之一</li><li>确认的密码套件列表，如RSA加密算法</li><li>服务器的数字证书 <img src="'+b+'" alt="image"></li></ul><p><strong>[客户端响应]:</strong></p><p>客户端收到服务器响应之后，首先需要通过浏览器或者操作系统中的CA公钥，确认服务器的数字证书的真实性。</p><p>如果证书中没有问题，客户端会从证书中拿到服务器的公钥，然后使用它来进行加密报文，向服务器发送下面这些信息：</p><ul><li>一个随机数<code>pre-master key</code>。该随机数会被服务器公钥加密</li><li>加密通信算法改变通知，表示随后的信息都会用“会话密钥”加密通信</li><li>客户端握手结束通知，表示客户端的握手阶段已经结束。这一项同时把之前所有内容的发生的数据做个摘要，用来给服务端校验</li></ul><p>此时服务器和客户端有了这三个随机数（<code>Client Random</code>，<code>Server Random</code>，<code>pre-master Key</code>），接着就用双方协商好的加密算法，各自生成本次通信的“会话密钥”</p><p><strong>[服务器最后响应]:</strong></p><p>服务器收到客户端的第三个随机数<code>pre-master key</code>之后，通过协商的加密算法，计算出本次通信的“会话密钥”，最后，向客户端发送最后的信息：</p><ul><li>加密通信算法改变通知，表示随后的信息都将用“会话密钥”加密通信</li><li>服务器握手结束通知，表示服务器的握手阶段已经结束。这一项同时把之前所有内容的发生的数据做个摘要，再用会话密钥加密，用来给客户端校验。</li></ul><h4 id="题外话" tabindex="-1">题外话 <a class="header-anchor" href="#题外话" aria-label="Permalink to &quot;题外话&quot;">​</a></h4><p><strong>[https一定安全吗？]</strong></p><p>假如客户端通过浏览器向服务端发起HTTPS请求时，被中间人拦住了，然后中间人拿到请求向服务端建立连接并转发这些请求，这样中间人相当于能够在中间完全监听到这个传输过程。</p><p>假如客户端通过浏览器向服务端发起HTTPS请求时，被中间人拦住了，然后中间人拿到请求向服务端建立连接并转发这些请求，这样中间人相当于能够在中间完全监听到这个传输过程。</p><p>但是一般来说，想要发生这种场景时有前提的，用户必须点击了接收了中间人服务器的证书，一般来说中间人会发生自己伪造的证书给浏览器，而这个伪造的证书能够被浏览器识别为非法的，于是会提示证书有问题</p><p><img src="'+S+'" alt="image"></p><p><strong>其实不能说https不安全，而是自己操作导致的（就好比你把密钥全部主动发给别人，也不能怪协议不安全吧💦）</strong></p><hr><p><strong>[为什么抓包工具可以抓到HTTPS的数据吗？]</strong></p><p>其实原理和中间人差不多，但是这种抓包工具会有自己的安全证书</p><h3 id="http2" tabindex="-1">HTTP2 <a class="header-anchor" href="#http2" aria-label="Permalink to &quot;HTTP2&quot;">​</a></h3><p>首先，需要知道HTTP/1.1还存在什么缺点：</p><ul><li>请求/响应头部（Header）未经压缩就发送，首部信息越多延迟越大。只能压缩<code>Body</code>的部分</li><li>发送冗长的首部。每次互相发送相同的首部造成的浪费较多</li><li>并发连接有限：谷歌浏览器的最大并发连接数是6个，而且每一个连接都要经过TCP握手</li><li>服务器是按请求的顺序响应的，如果服务器响应慢，会导致客户端一直请求不到数据，也就是队头阻塞</li><li>没有请求优先级控制</li><li>请求只能从客户端开始做起，服务器只能被动响应</li></ul><p>而HTTP是基于HTTPS，所以本身安全性有一定的保障，并且HTTP/2相比于HTTP/1.1性能上也有一定的改进:</p><ul><li>头部压缩</li><li>二进制格式</li><li>并发传输</li><li>服务器主动推送资源</li></ul><h4 id="头部压缩" tabindex="-1">头部压缩 <a class="header-anchor" href="#头部压缩" aria-label="Permalink to &quot;头部压缩&quot;">​</a></h4><p>HTTP/2会压缩头部，如果同时发出了多个请求，它们的头是一样或者是相似的，那么协议会<strong>消除重复的部分</strong>。这就是<code>HPACK</code>算法：在客户端和服务端同时维护一张头信息表，所有字段都会存入这个表，生成一个索引号，以后就不发送同样字段了，只需要更加简短的索引号，这样就提高速度了。 <img src="'+k+'" alt="image"> 表中的<code>Index</code>表示索引（Key）,<code>Header Value</code>表示索引对应的Value，<code>Header Name</code>表示字段的名字，比如Index为2代表 GET，Index为8代表状态码200。</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>静态表：对常见得key-value做了归类，比如<code>status:200</code>,<code>method:GET</code></p><p>动态表：对于请求中出现的重复字段和内容进行动态的记录，前提是必须同一个连接上，重复传输完全相同的HTTP头部</p></div><h4 id="二进制分帧" tabindex="-1">二进制分帧 <a class="header-anchor" href="#二进制分帧" aria-label="Permalink to &quot;二进制分帧&quot;">​</a></h4><p>HTTP/2 不再像 HTTP/1.1 里的纯文本形式的报文，而是全面采用了二进制格式，头信息和数据体都是二进制，并且都被统称为frame：<strong>头信息帧（Headers Frame）和数据帧（Data Frame）</strong></p><p><img src="'+_+'" alt="image"></p><p>虽然对阅读不太友好，但是对于计算机来说非常友好，不需要将报文转成二进制了，而是直接解析报文，增加了数据传输的效率。</p><p>比如状态码200，在HTTP/1.1是用&quot;2&quot;&quot;0&quot;&quot;0&quot;三个字符（二进制：00110010 00110000 001100000）来进行表示的</p><p><img src="'+q+'" alt="image"></p><h4 id="并发传输" tabindex="-1">并发传输 <a class="header-anchor" href="#并发传输" aria-label="Permalink to &quot;并发传输&quot;">​</a></h4><p>HTTP/1.1 的实现是基于请求-响应模型的。同一个连接中，HTTP完成一个事务，才能去处理下一个事务，也就是说在发出请求等待响应的过程中，是没办法做其他事情的，如果响应迟迟不来，那么后续的无法请求，造成了<strong>队头阻塞</strong>的问题。</p><p>而HTTP/2引出了Stream概念，多个Stream复用在一条TCP连接上。</p><p><img src="'+C+'" alt="image"></p><p>1个TCP连接包含多个Stream，Stream 里可以包含1个或多个Message，Message对应HTTP/1中的请求和响应，由HTTP头部和包体构成。Message里包含一条或者多个Frame，Frame是HTTP/2最小的单位，以二进制压缩格式存放在HTTP/1中的内容</p><p>可以得出一个结论：多个Stream跑在一条TCP连接上，同一个HTTP请求与响应是跑在同一个Stream中，HTTP消息可以由多个Frame构成，一个Frame可以由多个TCP报文构成</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>针对不同的HTTP请求用独一无二的Stream ID来区分，接收端可以通过Stream ID有序组装成HTTP消息，不同的Stream的帧是可以乱序发送的，因此可以并发不同的Stream，也就是HTTP/2可以并行交错地发送请求和响应</p></div><p><img src="'+f+'" alt="image"></p><p>如上图，服务端<strong>并行交错地</strong>发送了两个响应：Stream 1 和 Stream3，这两个 Stream 都是跑在一个TCP连接上，客户端收到之后，会根据相同的Stream ID 有序组装成HTTP消息。</p><p>HTTP/2通过Stream实现的并发，比HTTP/1.1实现的并发要厉害得多，<strong>因为当HTTP/2实现100个并发Stream时，只需要建立一次TCP连接，而HTTP/1.1需要建立100个TCP连接。</strong></p><h4 id="服务器推送" tabindex="-1">服务器推送 <a class="header-anchor" href="#服务器推送" aria-label="Permalink to &quot;服务器推送&quot;">​</a></h4><p>HTTP/2在一定程度上改善了传统的【请求-应答】工作模式，服务端不再是被动地响应，可以<strong>主动</strong>向客户端发送消息</p><p>客户端和服务端双方<strong>都可以建立Stream</strong>，Stream ID 也是有区别的，客户端建立的 Stream 必须是奇数号，而服务器建立的Stream必须是偶数号</p><p>比如下图，Stream 1是客户端向服务端请求的资源，属于客户端建立的 Stream，所以该 Stream 的 ID 是奇数；Stream 2 和 Stream 4 都是服务端主动向客户端推送的资源，属于服务端建立的 Stream , 所以这两个Stream ID是偶数</p><p><img src="'+I+'" alt="image"></p><p>再比如，客户端通过HTTP/1.1请求从服务器那获取到了HTML文件，而HTML可能还需要依赖CSS来渲染页面，这时客户端还要再发起获取CSS文件的请求，需要两次消息往返，如图：</p><p><img src="'+v+'" alt="image"></p><p>这样服务器可以主动推送一些文件，减少了消息传递的次数</p><h4 id="缺点" tabindex="-1">缺点 <a class="header-anchor" href="#缺点" aria-label="Permalink to &quot;缺点&quot;">​</a></h4><p>HTTP/2是基于TCP协议来传输数据的，TCP是字节流协议，TCP层必须保证收到的字节数据是完整且连续的，这一内核才会将缓冲区里的数据返回给HTTP应用，那么当【前一个字节数据】没有到达时，后收到的字节数据只能放在缓冲区里，只要等到这一个1字节数据到达时，HTTP/2应用层才能从内核种拿到数据，<strong>这就形成了HTTP/2队头阻塞问题。</strong></p><p><img src="'+x+'" alt="image"></p><p>一旦丢包发生，一个TCP连接种的<strong>所有的HTTP请求都必须等待这个丢了的包被重传回来。</strong></p><h3 id="http-3-0" tabindex="-1">HTTP/3.0 <a class="header-anchor" href="#http-3-0" aria-label="Permalink to &quot;HTTP/3.0&quot;">​</a></h3><p>通过前文可以知道HTTP/1.1和HTTP/2都存在队头阻塞的问题：</p><ul><li>HTTP/1.1是HTTP协议层面上的队头阻塞：虽然解决了请求的队头阻塞，但是<strong>没有解决响应的队头阻塞</strong>，因为服务端需要按顺序响应收到的请求，如果服务端处理某个请求消耗的时间较长，那么只能等响应完这个请求之后，才能处理下一个请求，这属于HTTP层的队头阻塞</li><li>HTTP/2是TCP协议层面上的队头阻塞：虽然通过多个请求复用一个TCP连接解决了HTTP的队头阻塞，但是<strong>一旦发生丢包，就会阻塞往所有的HTTP请求</strong></li></ul><p>HTTP/2的队头阻塞根本原因是TCP协议，所以<strong>HTTP/3直接切换TCP协议为UDP！</strong><img src="'+D+'" alt="image"></p><p>UDP 发送是不管顺序，也不管丢包的，所以不会出现像 HTTP/2 队头阻塞的问题。大家都知道 UDP 是不可靠传输的，但基于 UDP 的 <strong>QUIC</strong> 协议 可以实现类似 TCP 的可靠性传输。</p><p>QUIC协议有这么三个特点：</p><ul><li>无队头阻塞</li><li>更快的连接建立</li><li>连接迁移</li></ul><h4 id="无队头阻塞" tabindex="-1">无队头阻塞 <a class="header-anchor" href="#无队头阻塞" aria-label="Permalink to &quot;无队头阻塞&quot;">​</a></h4><p>QUIC协议也有类似HTTP/2 Stream与多路复用的概念，也是可以在同一条连接上并发传输多个Stream，Stream可以认为就是一条HTTP请求</p><p>QUIC协议有自己的一套机制可以保证传输的可靠性。<strong>当某个Stream发生丢包时，只会阻塞这个流，其他Stream不受影响。</strong></p><p>所以QUIC连接上的多个Stream之间并没有依赖，都是独立的，某个流发生丢包了，只会影响该流，其他流不受影响，而丢包的流必须等待QUIC重传丢失的包，才会将数据交给HTTP/3</p><p><img src="'+y+'" alt="image"></p><h4 id="更快的连接建立" tabindex="-1">更快的连接建立 <a class="header-anchor" href="#更快的连接建立" aria-label="Permalink to &quot;更快的连接建立&quot;">​</a></h4><p>在HTTP/1和HTTP/2协议种，由于TCP和TLS层是分层的，所以必须先TCP握手之后才能TLS握手</p><p>HTTP/3在传输数据前虽然需要QUIC协议握手，但是这个握手过程只需要1RTT，握手的目的是为了确认双方的<strong>连接ID</strong>，连接迁移就是基于连接ID来实现的</p><p>但是HTTP/3的QUIC协议并不是与TLS分层，而是QUIC内部包含了TLS，他在自己的帧种可以携带TLS信息，那么也就在1个RTT中完成建立连接与密钥协商</p><p><img src="'+E+'" alt="image"></p><h4 id="连接迁移" tabindex="-1">连接迁移 <a class="header-anchor" href="#连接迁移" aria-label="Permalink to &quot;连接迁移&quot;">​</a></h4><p>TCP连接中，传输协议基于HTTP协议，通过四元组（源IP，源端口，目的IP，目的端口）来确定了一条TCP连接。</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>当移动设备的网络从4G切换到WIFI时，就会意味着IP地址变化了，那么就必须要断开连接，然后重新建立连接。</p><p>而建立连接的过程包含TCP三次握手，TLS四次握手的时延，以及TCP慢启动的减速过程，给用户的感觉就是网络突然卡顿了一下，因此连接的迁移成本很高</p></div><p>而 QUIC 协议没有用四元组的方式来“绑定”连接，而是通过<strong>连接 ID</strong> 来标记通信的两个端点，客户端和服务器可以各自选择一组 ID 来标记自己，因此即使移动设备的网络变化后，导致 IP 地址变化了，只要仍保有上下文信息（比如连接 ID、TLS 密钥等），就可以“无缝”地复用原连接，消除重连的成本，没有丝毫卡顿感，达到了<strong>连接迁移</strong>的功能。</p><p>所以，QUIC是一个在UDP之上的 <strong>伪</strong>TCP + TLS + HTTP/2 的多路复用的协议</p>',196),N=[U];function R(A,F,Q,M,G,j){return s(),a("div",null,N)}const w=t(L,[["render",R]]);export{z as __pageData,w as default};
